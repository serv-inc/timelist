{
  "type": "object",
  "properties": {
    "blocks": {
      "description": "list of other block objects with time and regex",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "whitelist": {"type": "string"},
          "blacklist": {"type": "string", "description": "all allowed if not provided"},
          "starttime": {"type": "string"},
          "endtime": {"type": "string"}
        }
      }
    }
  }
}
